# Мини-планировщик тренировок (Workout Instruction Manager)

Это Pet-проект, Backend-сервис для создания и управления планами тренировок, написанный на Golang.

## 🚀 Как запустить проект

Этот проект использует Go, PostgreSQL и Docker (для базы данных).

**1. Клонировать репозиторий:**
git clone [https://github.com/legosinaas-art/-Workout-Instruction-Manager.git](https://github.com/legosinaas-art/-Workout-Instruction-Manager.git)
cd -Workout-Instruction-Manager

2. Настроить окружение (.env): Скопируйте файл-шаблон .env.example в новый файл .env:
Bash

cp .env.example .env

Откройте .env и впишите свой пароль для DB_PASSWORD (например, fit).

3. Запустить базу данных (Docker): Убедитесь, что у вас установлен и запущен Docker. Затем выполните:
Bash

docker-compose up -d

Эта команда поднимет контейнер с PostgreSQL, используя настройки из вашего .env файла.

4. Применить миграции:

    Откройте ваш любимый SQL-клиент (например, DBeaver или DataGrip).

    Подключитесь к localhost:5432 с юзером fit, паролем (из .env) и базой fit.

    Скопируйте и выполните весь SQL-код из файла migration/0001_init_schema.up.sql.

5. Запустить Go-сервер:
Bash

go run ./cmd/main.go

Сервер будет запущен на http://localhost:8080.

⚙️ Технологии

    Go (Golang)

    PostgreSQL

    Gin (для роутинга)

    sqlx (для работы с БД)

    Docker (для окружения)